create database Coalesce;
use Coalesce;
Create Table users (
userid int NOT NULL AUTO_INCREMENT,
username varchar(500),
email varchar(500),
password varchar(50);
verify boolean,
PRIMARY KEY (userid)
);






